export interface IConfig {
  darkMode: boolean;
  pallette: Pallette;
  useBackground: boolean;
}

export enum Pallette {
  RED,
  GREEN,
  BLUE,
  YELLOW,
  CYAN,
  MAGENTA,
  ORANGE,
  CHARTREUSE,
  AZURE,
  VIOLET,
  ROSE
}
